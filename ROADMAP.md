# QuickMedia Roadmap

## v1 — 已完成 ✅

- 扫描索引（SHA256 去重 + inode 追踪）
- 元数据提取（图片尺寸、视频分辨率/时长、音频信息）
- AI 图片分析（场景描述 + 元素标签）
- AI 视频首帧分析
- AI 文档摘要 + 关键词
- 全文搜索（LIKE 匹配）
- 手动标签管理
- Web UI（网格/列表视图、详情面板、标签编辑、暖色调设计）
- fsevents 实时文件监听
- 缩略图异步生成
- 设置页（Ollama 配置）

## v2 — 已完成 ✅

- OCR 图片文字提取
- 视频多帧采样分析（默认 1 帧，可配置）
- AI 分析异步化（任务队列，不阻塞扫描）
- 搜索结果关键词高亮
- Finder 打开按钮（路径旁文件夹图标）
- 视频帧数配置（设置页）
- AI 重试按钮（失败任务一键重置）
- AI 状态显示（网格/列表视图字段）
- Ollama 超时配置（设置面板，默认 300s）
- AI 重试策略（3 次立即重试，2s 间隔）
- 缩略图缓存破坏参数（?t= 时间戳）

---

## v3 — 语音识别 + 分析增强

### 语音识别（视频 + 音频素材）

使用 whisper 模型对视频中的语音和音频文件进行转录，输出：

- **语音转录原文** — 完整转录文本存储，可被搜索索引命中
- **语音 AI 标签** — 基于转录文本提取主题标签（如「会议」「采访」「教程」）
- **语音 AI 摘要** — 基于转录文本生成内容摘要

对视频素材，在语音分析之上叠加画面分析结果：

- **视频综合总结** — 语音摘要 + 多帧画面分析的标签/描述进行融合，生成视频整体总结（如「这是一段关于 X 的会议录像，画面中出现了...」）

```
视频素材分析链：
  语音轨道 ─→ whisper 转录原文
                  ├─→ 语音 AI 标签
                  └─→ 语音 AI 摘要
  画面轨道 ─→ 多帧采样+OCR
                  ├─→ 视觉 AI 描述
                  └─→ 视觉 AI 标签
                        ↓
                  视频综合总结（语音 + 视觉融合）

音频素材分析链：
  音频文件 ─→ whisper 转录原文
                  ├─→ AI 标签
                  └─→ AI 摘要
```

### 重新分析功能

- 素材记录最近一次 AI 分析完成时间和分析类型
- 详情面板提供「重新分析」按钮，主动触发指定素材的 AI 重新分析（含语音识别）
- 支持批量重新分析（选中多个素材一次性触发）

---

## 待定

### 素材管理与筛选

- **筛选方式重构** — 时长搜索、创建时间筛选、文件格式等基础属性从标签中分离为独立筛选组件；现有标签按语义初步分类（景物、动物、人物、物品、场景等）
- **素材预览** — 在 Web UI 中点击素材可直接打开文件预览（图片全屏、视频播放、文档查看），无需跳转到 Finder
- **多路径扫描配置** — 支持在 Web UI 设置面板中配置多个监控路径，每个路径可独立设置递归深度和分析开关
- 重复文件展示（显示副本路径）
- 素材版本历史查看
- 已删除素材回收站视图

### AI 分析增强

- **自定义 AI Prompt** — 用户可在设置中配置 AI 分析的 prompt 模板，自定义分析的输出内容和标签偏好
- **内容识别规则** — 用户可定义识别规则：指定要检测的内容类型（人物、动物、物品、场景等），配置匹配后自动打上的标签名称（如人名、宠物名）
- **语义相似度搜索** — embedding 向量相似度搜索
- **智能聚类推荐** — 系统自动分析素材内容，识别重复出现的人物/动物/物体并聚类分组；将聚类结果推荐给用户，一键添加到内容识别规则库中

### 分发与协作

- **打包分发** — PyInstaller 打包为 .app
- 外接硬盘 / NAS 支持
- 多人协作 / 云同步
- 移动端 App
- 素材导出 / 打包下载
