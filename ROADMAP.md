# QuickMedia Roadmap

## v1 — 已完成 ✅

- 扫描索引（SHA256 去重 + inode 追踪）
- 元数据提取（图片尺寸、视频分辨率/时长、音频信息）
- AI 图片分析（场景描述 + 元素标签）
- AI 视频首帧分析
- AI 文档摘要 + 关键词
- 全文搜索（LIKE 匹配）
- 手动标签管理
- Web UI（网格/列表视图、详情面板、标签编辑、暖色调设计）
- fsevents 实时文件监听
- 缩略图异步生成
- 设置页（Ollama 配置）

## v2 — 已完成 ✅

- OCR 图片文字提取
- 视频多帧采样分析（默认 1 帧，可配置）
- AI 分析异步化（任务队列，不阻塞扫描）
- 搜索结果关键词高亮
- Finder 打开按钮（路径旁文件夹图标）
- 视频帧数配置（设置页）
- AI 重试按钮（失败任务一键重置）
- AI 状态显示（网格/列表视图字段）
- Ollama 超时配置（设置面板，默认 300s）
- AI 重试策略（3 次立即重试，2s 间隔）
- 缩略图缓存破坏参数（?t= 时间戳）

## v3 — 已完成 ✅

- whisper 语音转录（faster-whisper small，视频+音频）
- 语音 AI 标签 + 摘要（基于转录文本）
- 视频综合总结（语音摘要 + 视觉分析融合）
- 重新分析功能（单个 + 批量多选）
- 素材删除（数据库移除，文件不受影响）
- 手动扫描（Web UI 一键扫描新素材）
- 转录文本可搜索（FTS 索引）
- analyzed_at 分析完成时间记录

---

## v4 — 已完成 ✅

- 类型筛选保留侧边栏展开式
- 创建时间 / 修改时间日期区间筛选
- 文件格式下拉多选（默认「点击筛选」，选中「已选 N 项」+ ✕）
- AI 状态下拉多选（交互同格式）
- 标签下拉多选（输入框展示标签名，取并集）
- 启动时清理旧时间/格式/类型标签
- Scanner 不再生成这些标签

---

## v5 — 自定义 AI Prompt ✅

让用户自定义 AI 分析维度，不同场景使用不同分析指令。核心设计：用户只编辑分析内容，输出格式由系统固定追加（JSON），确保解析可靠。

### 核心功能

- **Prompt 模板编辑** — 设置面板中编辑各分析类型的分析指令（图片/文档/语音/视频），格式尾巴系统固定追加
- **模板预设** — 提供预设模板（vision: 摄影/设计/宠物/人物，text: 技术文档/笔记日记/学习总结，speech: 会议记录/采访对话/学习总结）
- **JSON 输出** — 统一要求 LLM 以 JSON 格式输出，解析稳定可靠
- **Ollama 优化** — `think: false` 关闭思考过程，响应速度提升 20x+
- **重新分析** — 清除旧描述和 auto 标签后重新入队，手动标签保留
- **AI 状态增强** — 无队列记录时检查 ai_description/ai_summary 判断是否已完成，未分析素材显示"待分析"

### 涉及文件

- `quickmedia/prompt_config.py` — PromptConfig 类，管理 prompts.yaml 读写
- `quickmedia/ai.py` — VisionAnalyzer / TextAnalyzer JSON 解析 + Ollama 优化
- `quickmedia/api/server.py` — GET/PUT /api/prompts，重新分析清旧标签，AI 状态逻辑
- `docs/v5/` — plan.md / design.md / tasks.md

---

## v6 — 多模型配置 ✅

支持配置多种 AI 模型提供方（本地 Ollama + 远端 OpenAI/DeepSeek/OpenRouter），不同分析任务可使用不同模型。

### 核心功能

- **Provider 管理** — 配置多个模型提供方，按协议适配（OpenAI 兼容 / Ollama 原生）
- **任务模型绑定** — 每种分析类型独立选择 provider + model
- **模型能力目录** — `models.yaml` 定义各 provider 支持的模型及能力标注
- **API Key 分离** — Key 存 `.env`，配置可安全分享
- **自动迁移** — 首次升级自动将旧 Ollama 配置迁移到新结构
- **Web UI** — 独立模型管理页面，设置面板入口

---

## v7 — MiniMax 支持 + 设置弹窗重构 ✅

### 核心功能

- **MiniMax 原厂支持** — 新增 minimax provider，6 个模型（M3/M2.7/M2.5/M2.1/M2/M1）
- **设置弹窗重构** — 模态弹窗替代侧边栏面板，基础配置/模型管理/AI 提示词三个 Tab
- **保存按钮激活** — 有修改才变亮，无修改保持浅色
- **Provider 删除确认** — 删除前弹窗确认

### 涉及文件

| 文件 | 新增/修改 |
|------|----------|
| `quickmedia/models.yaml` | 修改 |
| `frontend/src/SettingsModal.tsx` | 新增 |
| `frontend/src/App.tsx` | 修改 |
| `frontend/src/ModelManager.tsx` | 修改 |

---

## v8 — 向量化 + 语义搜索 ✅

### 核心功能

- **分字段向量化** — 每个素材存 description/tags/text 3 个向量，best-field 取最小距离
- **RRF 融合搜索** — BM25 关键词 + 语义向量混合排序，jieba 中文分词
- **语义搜索** — 三种模式（综合/语义/匹配），无距离过滤，全部排序展示
- **相似素材推荐** — 详情页叠加层，按描述向量匹配
- **ChromaDB** — 轻量开源向量数据库，内嵌模式零配置
- **多模型** — Ollama qwen3-embedding:8b + OpenRouter qwen/qwen3-embedding-8b

---

## v9 — 语义搜索优化 ✅

### 核心功能

- **search_terms 字段** — AI 输出专注检索意图的搜索词（5-10个），与 tags 分离
- **独立向量化** — 每个 search_term 存独立向量，Top-K 聚合匹配
- **视频字段重构** — ai_description 重命名为 visual_description
- **Prompt 增强** — system_format 固定 search_terms 生成规则

### 素材管理

- **素材预览** — Web UI 直接打开文件预览（图片全屏、视频播放、文档查看）
- **多路径扫描配置** — Web UI 设置面板中配置多个监控路径
- 重复文件展示（显示副本路径）
- 素材版本历史查看
- 已删除素材回收站视图

### AI 分析增强

- **自定义 AI Prompt** — 用户可配置 AI 分析的 prompt 模板
- **内容识别规则** — 用户定义识别规则，匹配后自动打标签
- **AI 语义搜索** — 通过 embedding 向量对素材进行语义搜索，用自然语言描述即可找到相关素材（如「找那张有猫的照片」）
- **智能聚类推荐** — 自动识别重复人物/物体并聚类推荐；支持按自定义主题（如「AI 学习相关」）自动归类素材
- **素材聚类** — 自动识别重复/相似素材并聚类分组，支持自定义主题标签

### Hermes 集成

- **MCP 工具接口** — 将 QuickMedia 素材搜索、分析能力封装为标准 MCP 工具，供 Hermes Agent 直接调用
- **对话式素材查询** — 在 Hermes 中通过自然语言对话查找、分析和管理素材

### 分发与协作

- **打包分发** — PyInstaller 打包为 .app
- 外接硬盘 / NAS 支持
- 多人协作 / 云同步
- 移动端 App
- 素材导出 / 打包下载

---

## 附：待定需求优先级分析（2026-06）

从日常使用场景出发（非技术门槛），优先级排序：

### 🔴 高优先级 —「装上就会用，用了离不开」

**1. MCP 服务对接**
通过 MCP 协议把素材管理能力暴露给 Hermes / Claude Code / Cursor 等 AI Agent。在对话中直接搜素材、取文件、找相似，Agent 自动决定用什么素材。6 个工具：search_assets、get_asset、list_assets、find_similar、add_asset、delete_asset。

**2. 智能聚类 + 素材聚类**
几百张截图、照片堆着没人整理。AI 自动分组：「这 30 张是同一个人」「这 15 张都是发票」「这 20 张是设计灵感」，一键命名归类。没这个能力，手动整理几百张素材根本没人会做。

### 🟡 中优先级 —「锦上添花」

- 内容识别规则 — 自定义 Prompt 实现后，部分是语法糖
- 重复文件展示 / 版本历史 / 回收站 — 整理型功能，轻度用户不敏感

### 🟢 低优先级 —「生态与分发」

- Hermes 集成 / MCP 工具 — 技术用户特供
- 多人协作 / 云同步 / 移动端 — 需要后台基础设施
- 打包分发 — 技术门槛，解决了才有非技术用户，但不影响已有用户


## v10 — 可配置扫描文件夹 ✅
- 浏览器文件夹选择器 + macOS Finder 集成
- 红点引导 + 首次自动弹配置
- 保存热加载 + 向后兼容迁移


## v11 — MCP 对话式素材管理 ✅
- FastMCP 驱动，stdio 传输
- 6 工具 + 批量操作 + Pydantic 结构化输出
- 共享搜索/资产操作模块，消除重复代码
- Hermes 3 行配置即用

## v12 — 素材聚合 ✅
- AI 自动聚类：全量分析 / 全量追加 / 追加分析
- 独立聚合线程 (daemon，按需执行)
- 多对多节点关系 (nodes + node_assets)
- 侧边栏双 Tab：搜索筛选 / 聚合节点
- 节点右键交互 + 手动添加素材
- 扫描弹窗 (配置路径/选择文件/选择文件夹)
- Scanner 重构 (_insert_asset + _ingest_file)

## v13 — 云图

- Cytoscape.js 力导向图可视化节点-素材-共享边
- 视图切换三按钮：☁ 云图 / ▦ 网格 / ☰ 列表
- 聚合节点按素材数量缩放 + 梯度着色 + 展开/折叠素材节点
- 共享边（粗细=共享素材数），未分配节点（前端虚拟）
- 素材节点按类型着色，zoom 自适应缩略图
- 搜索高亮 + WebSocket 增量推送 + 状态跨视图保留
- 43 tests (28 backend + 15 frontend)

## v14 — 节点增强 + MCP 完善

- 节点分析追加（右键节点，AI 自动匹配未连接素材）
- 手动创建聚合节点（名称 + 描述 + 保存并分析）
- 修复节点删除（asyncio 冲突 + 选中清理）
- 统一弹框风格（Toast + ConfirmModal 替换原生 alert/confirm）
- 去掉刷新时配置引导弹窗
- 手动移除节点素材（右键菜单，复用添加素材弹框）
- 云图视觉增强（节点内素材计数、颜色深度梯度、尺寸放大）
- MCP 扩展至 21 个工具（节点 CRUD + 聚合 + 扫描 + 标签 + 统计 + 重分析）

## v15 — AI 搜索 + 节点树状列表 ✅

- AI 搜索模式（纯 LLM 单次调用，全量素材进 prompt，1M 上下文模型）
- 搜索模式重命名（综合→语义（K聚合），语义→语义（纯向量），新增 AI 排首位）
- search_ai prompt 模板加入 prompts.yaml + task_models 绑定
- NodePanel 侧边栏树形结构（独立箭头展开/折叠，按需加载素材）
- 未分配虚拟节点（树末尾，灰色/虚线，支持拖放操作）
- 跨组件拖放联动（NodePanel ↔ GraphView ↔ 网格/列表）
- 树列表变更刷新联动（聚合完成/节点增删/分析追加后即时更新）

## 待定

- 云图拖放素材到聚合节点（自动连线，不刷新全图）
