# QuickMedia Roadmap

## v1 — 已完成 ✅

- 扫描索引（SHA256 去重 + inode 追踪）
- 元数据提取（图片尺寸、视频分辨率/时长、音频信息）
- AI 图片分析（场景描述 + 元素标签）
- AI 视频首帧分析
- AI 文档摘要 + 关键词
- 全文搜索（LIKE 匹配）
- 手动标签管理
- Web UI（网格/列表视图、详情面板、标签编辑、暖色调设计）
- fsevents 实时文件监听
- 缩略图异步生成
- 设置页（Ollama 配置）

## v2 — 已完成 ✅

- OCR 图片文字提取
- 视频多帧采样分析（默认 1 帧，可配置）
- AI 分析异步化（任务队列，不阻塞扫描）
- 搜索结果关键词高亮
- Finder 打开按钮（路径旁文件夹图标）
- 视频帧数配置（设置页）
- AI 重试按钮（失败任务一键重置）
- AI 状态显示（网格/列表视图字段）
- Ollama 超时配置（设置面板，默认 300s）
- AI 重试策略（3 次立即重试，2s 间隔）
- 缩略图缓存破坏参数（?t= 时间戳）


## v3 — 远期

- 重复文件展示（显示副本路径）
- 素材版本历史查看
- 已删除素材回收站视图
- 视频语音转录（whisper）
- 视频整体总结（语音转录 + 多帧画面综合分析）
- 音频转录 + 摘要
- 语义相似度搜索（embedding 向量）
- 打包分发（PyInstaller .app）

## 待定

- 外接硬盘 / NAS 支持
- 多人协作 / 云同步
- 移动端 App
- 素材导出 / 打包下载
- **筛选方式重构** — 时长搜索、创建时间筛选、文件格式等基础属性从标签中分离为独立筛选组件；现有标签按语义初步分类（景物、动物、人物、物品、场景等）
- **自定义 AI Prompt** — 用户可在设置中配置 AI 分析的 prompt 模板，自定义分析的输出内容和标签偏好
- **重新分析功能** — 素材记录最近一次 AI 分析完成时间；详情面板提供「重新分析」按钮，主动触发现有素材的 AI 重新分析
- **多路径扫描配置** — 支持在 Web UI 设置面板中配置多个监控路径，每个路径可独立设置递归深度和分析开关
