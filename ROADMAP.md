# QuickMedia Roadmap

## v1 — 已完成 ✅

- 扫描索引（SHA256 去重 + inode 追踪）
- 元数据提取（图片尺寸、视频分辨率/时长、音频信息）
- AI 图片分析（场景描述 + 元素标签）
- AI 视频首帧分析
- AI 文档摘要 + 关键词
- 全文搜索（LIKE 匹配）
- 手动标签管理
- Web UI（网格/列表视图、详情面板、标签编辑、暖色调设计）
- fsevents 实时文件监听
- 缩略图异步生成
- 设置页（Ollama 配置）

## v2 — 已完成 ✅

- OCR 图片文字提取
- 视频多帧采样分析（默认 1 帧，可配置）
- AI 分析异步化（任务队列，不阻塞扫描）
- 搜索结果关键词高亮
- Finder 打开按钮（路径旁文件夹图标）
- 视频帧数配置（设置页）
- AI 重试按钮（失败任务一键重置）
- AI 状态显示（网格/列表视图字段）
- Ollama 超时配置（设置面板，默认 300s）
- AI 重试策略（3 次立即重试，2s 间隔）
- 缩略图缓存破坏参数（?t= 时间戳）

## v3 — 已完成 ✅

- whisper 语音转录（faster-whisper small，视频+音频）
- 语音 AI 标签 + 摘要（基于转录文本）
- 视频综合总结（语音摘要 + 视觉分析融合）
- 重新分析功能（单个 + 批量多选）
- 素材删除（数据库移除，文件不受影响）
- 手动扫描（Web UI 一键扫描新素材）
- 转录文本可搜索（FTS 索引）
- analyzed_at 分析完成时间记录

---

## v4 — 筛选方式重构

将基础属性（时间、格式）从标签中分离为独立筛选组件。当前问题：创建时间、文件格式等以标签形式存在，标签列表冗长，筛选体验差。

### 筛选组件

- **创建时间区间** — 日期范围选择器（开始 ~ 结束），默认全部，支持快捷选项（今天、本周、本月）
- **修改时间区间** — 日期范围选择器（开始 ~ 结束），默认全部
- **文件格式** — 弹出多选面板（jpg, png, mp4, wav, pdf 等），默认全不选 = 全部，勾选则筛选
- **标签** — 弹出多选面板，默认全不选 = 全部，勾选取交集筛选
- **AI 状态** — 弹出多选面板（等待分析、分析中、已完成、失败），默认全不选 = 全部，勾选则筛选

### 标签清理

- 移除现有自动生成的时间标签（如「2026」「2026-06」），由创建时间筛选替代
- 移除现有自动生成的格式标签（如「PNG」「MP4」），由文件格式筛选替代
- 移除现有自动生成的类型标签（如「图片」「视频」），侧边栏已有类型筛选
- 保留自动生成的时长段标签（如「短片」「中片」「长片」）在标签系统中
- AI 生成的标签和用户手动标签保持不变

### 交互设计

- 筛选栏置于素材列表上方，与搜索框、视图切换同行
- 每个筛选组件旁边显示已选数量（如「格式 (3)」）
- 筛选条件变化时即时生效（无需点击确认）
- 已激活的筛选条件用珊瑚色标记

---

## 待定

### 素材管理

- **素材预览** — Web UI 直接打开文件预览（图片全屏、视频播放、文档查看）
- **多路径扫描配置** — Web UI 设置面板中配置多个监控路径
- 重复文件展示（显示副本路径）
- 素材版本历史查看
- 已删除素材回收站视图

### AI 分析增强

- **自定义 AI Prompt** — 用户可配置 AI 分析的 prompt 模板
- **内容识别规则** — 用户定义识别规则，匹配后自动打标签
- **语义相似度搜索** — embedding 向量相似度搜索
- **智能聚类推荐** — 自动识别重复人物/物体并聚类推荐

### 分发与协作

- **打包分发** — PyInstaller 打包为 .app
- 外接硬盘 / NAS 支持
- 多人协作 / 云同步
- 移动端 App
- 素材导出 / 打包下载
